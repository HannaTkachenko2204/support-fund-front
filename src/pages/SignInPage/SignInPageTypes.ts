// Тип даних для форми входу (SignIn)
export interface SignInFormData {
    email: string;
    password: string;
  }
  
  // Пропси для сторінки входу (якщо треба в майбутньому)
  export interface SignInPageProps {
    // приклад: redirectUrl?: string;
  }
  